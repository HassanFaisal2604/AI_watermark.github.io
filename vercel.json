{
    "version": 2,
    "builds": [{
        "src": "server.py",
        "use": "@vercel/python",
        "config": {
            "runtime": "python3.9",
            "buildCommand": "bash build.sh"
        }
    }],
    "routes": [{ "src": "/(.*)", "dest": "server.py" }]
}